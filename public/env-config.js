// Este archivo es generado automáticamente por Docker en producción
// Para desarrollo local, las variables se leen desde .env usando Vite
// No editar manualmente este archivo

// Inicializar window.__ENV__ vacío para desarrollo
if (typeof window !== 'undefined' && !window.__ENV__) {
  window.__ENV__ = {};
}
